role="Admin"
section=1


score_override = [] #tuples of (regex, x); questions matching regex will receive a score of x
extensions = [] #tuples of (regex, n); questions matching regex will receive an extension of n weeks (floats okay)
excused = [] #regex strings; questions matching regex will be excused (normally reserved for midterms)
nsubmits_extra = [] #tuples of (regex, n); questions matching regex will receive n extra checks
